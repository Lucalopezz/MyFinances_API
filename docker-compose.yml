services:
  mongodb:
    image: mongo:7
    container_name: myfinances-mongo
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - /mnt/dados/docker-volumes/myfinances-mongo:/data/db
    restart: unless-stoppedn